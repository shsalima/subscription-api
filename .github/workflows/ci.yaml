on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      mongodb:
        image: mongo:6.0
        ports:
          - 27017:27017

    steps:
      - name: Checkout/pull the code
        uses: actions/checkout@v7.0.0

      - name: Set node.js environment
        uses: actions/setup-node@v6.4.0
        with:
          node-version: 24

      - name: Install the dependencies
        run: npm ci

      - name: Test the code
        run: npm test
        env:
          PORT: 3001
          MONGODB_URL: mongodb://localhost:27017/Abonnement-api
          JWTS: fb433489a7e8
